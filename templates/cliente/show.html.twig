{% extends 'base.html.twig' %}

{% block title %}Cliente{% endblock %}

{% block body %}
    <h1 class="h1titulosdepaginas">Cliente</h1>
<div class="tabladeeditar">

    <table id="tabladiseño" class="table">
        <tbody>
            <tr>
                <th>Idcliente</th>
                <td>{{ cliente.idcliente }}</td>
            </tr>
            <tr>
                <th>Dni</th>
                <td>{{ cliente.dni }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cliente.nombre }}</td>
            </tr>
            <tr>
                <th>Apellido</th>
                <td>{{ cliente.apellido }}</td>
            </tr>
            <tr>
                <th>Ciudad</th>
                <td>{{ cliente.ciudad }}</td>
            </tr>
        </tbody>
    </table>
</div>
<div class="diveditar">
    <a class="botonregresar" href="{{ path('cliente_index') }}">Regresar</a>

    <a class="botoneditar" href="{{ path('cliente_edit', {'idcliente': cliente.idcliente}) }}">Editar</a>

    {{ include('cliente/_delete_form.html.twig') }}
</div>

{% endblock %}
